.index-state.json
index-accelerator.log
.DS_Store

# backend secrets (never commit)
api/config.local.php
.imggen-state.json

# Фото-карточки: webp нужны сайту, PNG/исходники — нет (гигабайты)
assets/cards-photo/*.png
assets/cards-photo/_raw/
assets/cards-photo/_gallery.html
