---
description: Stage 2 — Синтез. Конверсионный аудит сайта + позиционирование + слепые зоны на основе отчётов Stage 1.
---

# /intel-stage2 — Синтез

Ты — оркестратор стадии 2. Стадия 1 должна быть уже выполнена.

Порядок:
1. Проверь, что в `intel/wiki/concepts/` есть отчёты Stage 1 (`analysis-*`). Если нет — попроси сначала запустить `/intel-stage1`.
2. Запусти субагентов через Task:
   - сначала `intel-synth-audit` (конверсионный аудит → `audit-conversion.md`),
   - затем `intel-synth-positioning` (позиционирование + слепые зоны → `positioning.md`, `blind-spots.md`).
3. Проверь, что три файла созданы. Кратко перескажи главное: топ-приоритеты аудита, суть позиции, ключевые слепые зоны.
4. Предложи запустить `/intel-stage3` в **новом диалоге**.

Не выдумывай фактов — синтез строго по вики. Не трогай боевой сайт.
